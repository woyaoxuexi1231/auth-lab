# ============================================================
# OAuth2 资源服务器 — 生产环境配置
# ============================================================
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: @oauth2.base-url@

