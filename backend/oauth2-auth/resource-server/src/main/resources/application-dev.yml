# ============================================================
# OAuth2 资源服务器 — 开发环境配置
# ============================================================
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: @oauth2.base-url@

